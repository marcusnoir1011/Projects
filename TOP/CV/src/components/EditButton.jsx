const EditButton = ({ toggleMode }) => {
    return (
        <>
            <button type="button" onClick={toggleMode}>
                Edit
            </button>
        </>
    );
};

export default EditButton;
