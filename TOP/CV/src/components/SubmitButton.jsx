const SubmitButton = ({ onSubmitClick }) => {
    return (
        <>
            <button type="button" onClick={onSubmitClick}>
                Submit
            </button>
        </>
    );
};

export default SubmitButton;
